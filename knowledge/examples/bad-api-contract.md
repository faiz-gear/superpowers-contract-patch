# Bad API Contract Example

Traits of a bad contract:

- missing or autogenerated `operationId`
- success response typed as `object`
- envelope described in prose but absent from schema
- frontend must guess field names or write custom adapters

Example failures:

- `200` schema says `object`
- DTO names do not match business meaning
- contract and implementation disagree on envelope fields
